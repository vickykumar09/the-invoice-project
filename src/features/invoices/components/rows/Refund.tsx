import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function RefundRow({props}: {props?: any}) {
  return (
    <View>
      <Text>RefundRow</Text>
    </View>
  )
}

const styles = StyleSheet.create({})